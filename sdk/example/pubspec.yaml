name: patchfly_example
description: Demo app for the Patchfly SDK
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  patchfly:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
