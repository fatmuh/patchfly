name: patchfly_server
description: Patchfly backend API — self-hosted code push for Flutter
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.1
  postgres: ^3.4.0
  dart_jsonwebtoken: ^2.8.0
  bcrypt: ^1.1.3
  uuid: ^4.2.0
  dotenv: ^4.2.0
  logging: ^1.2.0
  collection: ^1.18.0
  path: ^1.9.0
  crypto: ^3.0.3
  args: ^2.4.0
  mime: ^1.0.4
  shelf_multipart: ^1.0.0
  pointycastle: ^3.9.1
  minio: ^3.5.0

dev_dependencies:
  test: ^1.24.0
  http: ^1.1.0
  lints: ^3.0.0
